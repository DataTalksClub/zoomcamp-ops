# Stripped Ordinal

The H1 lost its ordinal but the declared title kept one: the half-migrated
state. The page's strip is an exact match, so this renders the title twice —
the M012 case that matters, and the reason U011's fix moves both sides.
