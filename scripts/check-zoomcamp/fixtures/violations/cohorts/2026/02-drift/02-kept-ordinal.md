# 2.2 Kept Ordinal

The H1 matches the declared title exactly, ordinal included, so the published
page still strips it — this renders correctly today. It is the un-migrated
state: U011 flags it, and the fix takes the ordinal off both sides at once.
